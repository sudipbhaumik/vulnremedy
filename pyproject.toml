[project]
name = "vulnremedy"
version = "0.1.0"
description = "AI-powered vulnerability remediation platform"
readme = "README.md"
requires-python = ">=3.11.9"
[tool.hatch.build.targets.wheel]
packages = ["src/vulnremedy"] 
dependencies = []